VISION 2030 CONTEMPORARY DATA-FED SITE

This package preserves the original captured UI. run_vision2030.py finds the newest timestamped workbook under the sibling output folder, updates the existing embedded VD metricData model, writes generated copies to the site root, and launches localhost.

Run:
& "..\.venv\Scripts\python.exe" ".\run_vision2030.py"

Safety:
- Original UI templates stay unchanged in templates\.
- The extraction workbook is read-only.
- Current phase updates enterprise metrics and direct division rows only.
- Existing region/facility UI data remains unchanged until its mapping is validated.
